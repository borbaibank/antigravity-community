"""Trading engine (entry, exit, sync, signals, protection, ops).

Submodules are imported directly (``bot.engine.entry``, etc.) to avoid import-order
issues with optional ``antigravity_pro`` registration.
"""

__all__: list[str] = []
